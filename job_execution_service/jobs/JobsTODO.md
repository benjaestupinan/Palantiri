## Future Job List

### Orquestacion
- job_sequence (list of jobs to be executed sequentially)

### Filesystem
- read_file (leer contenido de un archivo dado un path)
- write_file (escribir contenido a un archivo dado un path)
- list_directory (listar archivos y carpetas en un directorio)

### HTTP
- http_request (hacer un GET/POST a una URL y devolver el response body)

### Busqueda
- grep_search (buscar un patron en archivos dentro de un directorio)

### Sistema
- get_environment_variable (leer una variable de entorno)
